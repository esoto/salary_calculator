<%# Replace the entire category column (item already removed, updates totals) %>
<% @budget.budget_items.reload %>
<%= turbo_stream.replace dom_id(@budget, @budget_item.category) do %>
  <%= render "budgets/category_column", budget: @budget, category: @budget_item.category %>
<% end %>
